# RoadLime — environment template
# Copy to .env.local and fill in values.

# HMAC secret for session cookie signing. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-me-with-32-bytes-of-hex

# Public base URL used in QR codes and SEO metadata.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Supabase (not wired this session; migration is ready under /supabase) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# --- Mapbox (deferred) ---
# NEXT_PUBLIC_MAPBOX_TOKEN=

# --- Payments (deferred — abstraction layer planned per spec) ---
# WIPAY_API_KEY=
# WIPAY_ACCOUNT_NUMBER=
# POWERTRANZ_PWT_ID=
# POWERTRANZ_PWT_PASSWORD=
